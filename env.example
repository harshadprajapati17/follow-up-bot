# Telegram Bot Token
# Get your token from @BotFather on Telegram
TELEGRAM_TOKEN=your_telegram_bot_token_here
# Chat ID where cron-job sends morning/evening voice (e.g. from @userinfobot or group ID)
TELEGRAM_CHAT_ID=your_telegram_chat_id_here
# Base URL of your deployed app (no trailing slash). Used by set-webhook.sh to register webhook.
# Examples: https://your-app.vercel.app  or  https://your-ngrok-url.ngrok.io
# TELEGRAM_WEBHOOK_BASE_URL=https://your-app.vercel.app

# Google Sheets API (Service Account)
# Create a service account in Google Cloud Console, enable Sheets API, then add:
GOOGLE_PROJECT_ID=your-google-cloud-project-id
GOOGLE_CLIENT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
# Private key from the JSON key file (keep the \n as literal for newlines, or use real newlines)
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# Default spreadsheet ID (from sheet URL: between /d/ and /edit). Used by /api/sheets when request omits spreadsheetId.
GOOGLE_SHEET_ID=1S3dQnwxONPHOmhC_yX6blb-8PYanM_-YEMVbz6_FAMY

# Sarvam AI (Text-to-Speech + Speech-to-Text)
# Get your key from Sarvam Dashboard → API Keys. Used by /api/tts and webhook STT.
SARVAM_API_KEY=your_sarvam_api_key_here
# STT model (e.g. saarika:v2.5, saaras:v3). Optional; Sarvam default used if unset.
# SARVAM_STT_MODEL=saaras:v3
# TTS speaker (e.g. hitesh, shubh) and model (e.g. bulbul:v2).
# SARVAM_TTS_SPEAKER=hitesh
# SARVAM_TTS_MODEL=bulbul:v2

# MongoDB (conversation-bot storage)
# Used by Telegram webhook to store /project conversation answers (work_location, rooms_count, assign_resources).
MONGODB_URI=mongodb://localhost:27017
# Optional: database name (defaults to "conversation-bot")
# MONGODB_DB_NAME=conversation-bot

# AWS S3 (voice message storage)
# Used by Telegram webhook to store voice messages.
AWS_REGION=ap-south-1
AWS_S3_BUCKET=your-bucket-name
S3_PUBLIC_BASE_URL=https://your-bucket-name.s3.ap-south-1.amazonaws.com
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# Google Gemini (lead analysis)
# Used by /api/lead/analyze to extract structured data from free-form text.
GEMINI_API_KEY=your_gemini_api_key_here
